import * as React from 'react';
import './index.scss';

export default class SkillsLayout extends React.Component<{}, {}> {
    render() {
        return (
            <div id='skills-layout' className='layout'>
                <span>Skills Layout</span>
            </div>
        );
    }
}
